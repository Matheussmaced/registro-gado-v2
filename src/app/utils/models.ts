/**
 * Poderia ser em outro lugar
 * Mas na arquitetura que pensei
 * fica mais fácil de entender aqui
 */

  export type Animal = {
    id: number;
  };


  export type AddAnimal = {
    id: number;
  };

  export type Params = {
    id: number;
  };